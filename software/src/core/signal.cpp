// signal.cpp - DBC signal conversion helpers
//
// raw_to_physical / physical_to_raw are defined inline in signal.h.
// This translation unit keeps the bt_core library shape stable and hosts
// future non-inline signal helpers (e.g. scaling/rounding policy).
#include "core/signal.h"

namespace bt {
// (inline implementations live in signal.h)
}  // namespace bt
